# Copy this file to .env and replace the value below before collecting.
# Any text of 32+ characters. Keep it forever: changing it breaks posting history.
# Left unedited this placeholder is too short, so make sweep stops instead of pinning a
# public key to your collection history.
OBSERVATORY_HMAC_KEY=CHANGE-ME
