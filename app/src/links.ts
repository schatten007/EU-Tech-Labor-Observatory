/**
 * The single place the app names the audit page.
 *
 * The recommended publish layout (see the A4 session note) serves the app's build
 * output at `docs/app/` directly beside the audit page at `docs/index.html`, so the
 * analytical fallback is always exactly one directory up. The link is relative on
 * purpose: it works under that layout without knowing the host or base path.
 */
export const AUDIT_PAGE_URL = '../index.html'
